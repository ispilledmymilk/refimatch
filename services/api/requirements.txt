fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5
python-dotenv>=1.0
asyncpg>=0.29

# Local monorepo installs (Dockerfile sets PYTHONPATH / pip -e)
# pip install -e ../../packages/scoring -e ../agent
